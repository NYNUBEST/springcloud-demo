spring:
  application:
    name: demo-feign
  cloud:
    config:
      uri: http://127.0.0.1:8888
    bus:
      trace:
        enabled: true

  rabbitmq:
    host: localhost
    port: 5672
    username: docker
    password: ding