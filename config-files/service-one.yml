server:
  port: 8081

spring:
  application:
    name: service-one

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org.springframework.cloud: DEBUG

